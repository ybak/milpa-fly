package com.corntree.milpa.fly.protocol.response;

option optimize_for = SPEED;

enum ResponseCode {
  OK = 0;
  ERROR_UNKOWN = 1;
  ERROR_BAD_PROTOCOL = 2;
}

message BaseResponse {
  required ResponseCode code = 1;
  optional string desc = 2;
}